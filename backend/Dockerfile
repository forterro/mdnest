# syntax=docker/dockerfile:1.4
# (the syntax line enables BuildKit cache mounts below)

# Stage 1: Build
# Bumped to 1.26 in v3.5.0 to clear stdlib CVEs in the 1.25 line
# (GO-2026-4865/4866/4870/4946/4947 — all crypto/x509, crypto/tls,
# html/template). The 1.26-alpine moving tag pulls the latest 1.26.x
# patch so we keep up with future fixes without manual bumps.
FROM golang:1.26.5-alpine AS builder

WORKDIR /app

# Cache the Go module download layer in a BuildKit cache mount so it
# persists across `--no-cache` rebuilds. First build pulls everything,
# subsequent ones reuse the cache → ~zero network time on rebuild.
COPY go.mod go.sum ./
RUN --mount=type=cache,target=/go/pkg/mod \
    go mod download

COPY . .

# Short git commit + build timestamp, passed by docker-compose (setup.sh
# computes them on the host since .git isn't in the build context). Baked into
# the binary via -ldflags below so /api/config reports the exact build — which
# commit and when it was built — that's running.
ARG GIT_COMMIT=unknown
ARG BUILD_TIME=unknown

# Two cache mounts:
#   /root/.cache/go-build — Go's incremental compile cache. Reusing this
#     across builds is the biggest win for our codebase post-v3.4.0
#     (firestore + grpc + protobuf are slow to compile from scratch).
#     Took us from ~180-235s clean down to ~10-30s when only mdnest
#     source has changed.
#   /go/pkg/mod — module cache, mirrored for the build step too.
# These cache mounts SURVIVE `--no-cache` because they're stored outside
# the image layers; only the layer hashes get invalidated.
RUN --mount=type=cache,target=/root/.cache/go-build \
    --mount=type=cache,target=/go/pkg/mod \
    CGO_ENABLED=0 GOOS=linux go build \
      -ldflags "-X github.com/mdnest/mdnest/backend/handlers.Commit=${GIT_COMMIT} -X github.com/mdnest/mdnest/backend/handlers.BuildTime=${BUILD_TIME}" \
      -o mdnest .

# Stage 2: Run
FROM alpine:latest

# tini becomes PID 1 (see ENTRYPOINT below) so orphaned grandchildren of a git
# subprocess (ssh, askpass, git-remote-https/http helpers that can outlive a
# killed or already-exited git parent) get reaped the instant they exit,
# instead of sitting as zombies until reapZombies' periodic sweep in main.go
# gets to them. The two are complementary: tini catches reparented orphans
# immediately; reapZombies is the backstop for anything tini's scope doesn't
# cover (e.g. a direct child of this process that never gets Wait()-ed).
RUN apk add --no-cache ca-certificates git openssh-client tini

WORKDIR /app

COPY --from=builder /app/mdnest .

EXPOSE 8080

ENTRYPOINT ["/sbin/tini", "--"]
CMD ["./mdnest"]
